/**
 *print_square  - check the code
 *@size :  is the size of the square
 * Return: Always 0.
 */
int main(void)
{
	int a, b;

	if (size > 0)
	{
		for (a = 0; a < size; a++)
		{
			for (b = 0; b < size; b++)
			{
				_putchar('#');
				_putchar('\n');
			}
		}
	}

		else
			_putchar('\n');
}	
